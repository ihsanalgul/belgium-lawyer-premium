# Decap CMS configuration
# DecapBridge: set backend to github + base_url per https://decapbridge.com/docs
backend:
  name: git-gateway
  branch: main
  accept_roles:
    - cms

# DecapBridge OAuth (uncomment and set repo after connecting at decapbridge.com):
# backend:
#   name: github
#   repo: YOUR_GITHUB_USER/belgium-lawyer-premium
#   branch: main
#   base_url: https://api.decapbridge.com
#   auth_endpoint: auth

# Enable only for local CMS dev with `npx decap-server`
# local_backend: true

media_folder: public/images/uploads
public_folder: /images/uploads

locale: tr

collections:
  - name: blog
    label: Blog Yazıları
    label_singular: Blog Yazısı
    folder: content/blog
    create: true
    slug: '{{slug}}'
    extension: md
    format: frontmatter
    fields:
      - { label: Slug, name: slug, widget: string, hint: 'URL parçası; boş bırakılırsa başlıktan otomatik üretilir' }
      - { label: Tarih, name: date, widget: datetime, date_format: 'YYYY-MM-DD', time_format: false }
      - { label: Etiketler, name: tags, widget: list, default: [] }
      - label: Türkçe
        name: tr
        widget: object
        fields:
          - { label: Başlık, name: title, widget: string, hint: 'Sayfa başlığı (H1); .md içindeki title alanından da alınabilir' }
          - { label: Özet, name: excerpt, widget: text, hint: 'Blog listesinde görünen kısa özet' }
          - { label: Meta Açıklama, name: metaDescription, widget: text, hint: 'Arama motorları için açıklama' }
          - { label: İçerik, name: body, widget: markdown, hint: 'Tam .md dosyasını yapıştırabilirsiniz. Kayıtta başlık, tarih ve açıklama otomatik ayrılır. Bölümler için ## kullanın.' }
      - label: English
        name: en
        widget: object
        required: false
        collapsed: true
        fields:
          - { label: Title, name: title, widget: string, required: false, hint: 'Opsiyonel — site şu an yalnızca Türkçe gösterir' }
          - { label: Excerpt, name: excerpt, widget: text, required: false }
          - { label: Meta Description, name: metaDescription, widget: text, required: false }
          - { label: Body, name: body, widget: markdown, required: false }
      - label: Français
        name: fr
        widget: object
        required: false
        collapsed: true
        fields:
          - { label: Titre, name: title, widget: string, required: false, hint: 'Optionnel — le site n''affiche que le turc pour l''instant' }
          - { label: Résumé, name: excerpt, widget: text, required: false }
          - { label: Meta, name: metaDescription, widget: text, required: false }
          - { label: Contenu, name: body, widget: markdown, required: false }
      - label: Nederlands
        name: nl
        widget: object
        required: false
        collapsed: true
        fields:
          - { label: Titel, name: title, widget: string, required: false, hint: 'Optioneel — de site toont momenteel alleen Turks' }
          - { label: Samenvatting, name: excerpt, widget: text, required: false }
          - { label: Meta, name: metaDescription, widget: text, required: false }
          - { label: Inhoud, name: body, widget: markdown, required: false }

  - name: pages
    label: Sayfalar
    files:
      - label: KVKK Aydınlatma Metni
        name: kvkk
        file: content/pages/kvkk.md
        fields:
          - { label: Başlık, name: title, widget: string }
          - { label: İçerik, name: body, widget: markdown }
